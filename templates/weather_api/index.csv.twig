city,country,date,celsius,fahrenheit,humidity,pressure
{% for measurement in measurements %}
{{ city }},{{ country }},{{ measurement.date|date('Y-m-d') }},{{ measurement.temperature }},{{ measurement.fahrenheit }},{{ measurement.humidity }},{{ measurement.pressure }}
{% endfor %}
